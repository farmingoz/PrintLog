apiVersion: v1
kind: Service
metadata:
  name: {{ template "printlogweb.fullname" . }}
  labels:
    app: {{ template "printlogweb.name" . }}
    chart: {{ template "printlogweb.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "printlogweb.name" . }}
    release: {{ .Release.Name }}
